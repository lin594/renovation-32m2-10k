# 装修项目协作约定

## 角色与语言

- 默认使用简体中文。
- 业主是项目所有者和实际策划人；助手负责整理事实、发现冲突、给出高性价比方案并维护项目资料。
- 不把推测写成已确认事实。字段和文档中明确使用 `confirmed`、`planned`、`todo`、`blocked`、`done`。

## 数据维护

- 户型、点位和设备变化更新 `house.yaml`。
- 每笔支出、收入、退款和待收款更新 `data/ledger.csv`；金额使用人民币元，收入为正向流入但单独记录 `flow`。
- 可复用材料和剩余材料更新 `data/inventory.yaml`。
- 阶段和任务更新 `data/project.yaml`。
- 施工窗口、日排期、人员边界和入住门槛更新 `data/schedule.yaml`。
- 燃气、强电、防水、排烟、承重、猫防逃等问题更新 `data/risks.yaml`。
- 影响较大的选择在 `docs/decisions/` 新建编号决策记录。
- `diagrams/` 只保留当前生效图纸；旧方案由 Git 历史追溯，不维护 `vN/` 或 `archive/` 副本。
- `PROJECT_STATUS.md` 和 `diagrams/*.svg` 是派生文件，分别由 `make status` 和 `make diagrams` 生成，不手工修改。
- 总体/家具图不绘制会遮挡通道的待定门扇；未解决的开启与停泊冲突放入门窗详图，用候选方案和净空尺寸表达。

## Git 约定

- 每个逻辑更新单独提交，提交前运行 `make check`。
- 推送前先确认远端没有新提交；不强推，不用自动化覆盖原始账本或主数据。
- 提交格式：`<类型>(<范围>): <中文摘要>`；类型、范围和摘要均优先使用中文。
- 类型：`建档|更新|修正|设计|记账|采购|进度|风险|规范|文档|维护`。
- 范围使用房间或装修领域名称，例如 `卫生间`、`厨房`、`弱电`、`预算`、`版本管理`；仅在不可替代的技术专名中使用英文。
- 小更新直接提交 `main`；多方案探索使用 `design/<topic>` 分支。
- 不重写已共享历史，不使用 `git reset --hard`，不强推。

## 安全边界

- 低成本不能覆盖燃气、强电、防水、结构、排烟和人宠安全要求。
- 漏电保护器、延时送电器等设备不得自动视为接地替代方案。
- 对无地线、浴霸触水、燃气管遮蔽、热水器排烟和大功率线路，只登记事实与风险；具体施工结论需现场检测并按所在地现行规范、燃气公司及持证人员要求确认。
- 图纸必须注明“讨论图/施工图”属性；未给尺寸的物体使用虚线或 `TBD`。

## 完成定义

一次更新只有在以下条件满足后才完成：数据已落盘、相关图纸或文档已同步、`make check` 通过、Git 状态清晰、已创建可读提交。
